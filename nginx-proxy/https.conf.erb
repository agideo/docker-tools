stream {
    server {
        listen 443;
        ssl_preread on;
        resolver <%= ENV.fetch('DNS_SERVER') { '8.8.8.8' } %> valid=3600s;
        proxy_pass $ssl_preread_server_name<%= ENV['BACK_DOMAIN'] %>:$server_port;
    }
}
